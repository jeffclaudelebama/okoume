"use client";
import { useState } from "react";
import { ProductCard } from "@/components/product-card";
import type { StorefrontProduct } from "@/lib/storefront-catalog";
const categories = [["tous", "Tous"], ["smartphones", "Smartphones"], ["accessoires", "Accessoires"], ["audio", "Audio"], ["electromenager", "Maison"]] as const;
export function CatalogBrowser({ products }: { products: StorefrontProduct[] }) { const [category, setCategory] = useState<string>("tous"); const visible = category === "tous" ? products : products.filter((product) => product.category === category); return <section className="pt-3"><p className="text-[10px] font-bold uppercase tracking-[0.18em] text-neutral-500">Catalogue</p><h1 className="mt-2 text-3xl font-bold tracking-tight">Rayons</h1><div className="mt-5 flex gap-2 overflow-x-auto pb-1">{categories.map(([id, label]) => <button key={id} onClick={() => setCategory(id)} className={`okoume-interactive shrink-0 rounded-full px-4 py-2 text-xs font-bold ${category === id ? "bg-black text-white dark:bg-white dark:text-black" : "bg-[#F5F5F7] dark:bg-[#141414]"}`}>{label}</button>)}</div><p className="mt-5 text-sm text-neutral-600 dark:text-neutral-400">{visible.length} produit{visible.length > 1 ? "s" : ""} disponible{visible.length > 1 ? "s" : ""}</p><div className="mt-3 grid grid-cols-2 gap-3 sm:grid-cols-3 lg:grid-cols-4">{visible.map((product) => <ProductCard key={product.id} {...product} />)}</div></section>; }
